#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../rolldown/bin/cli.mjs" "$@"
else
  exec node  "$basedir/../../../rolldown/bin/cli.mjs" "$@"
fi
# cmd-shim-target=/Users/declanallen/.config/valet/Sites/GitHub/hiphopopotamuss.github.io/node_modules/.pnpm/vite@8.0.6_@types+node@24.12.2_sass@1.99.0/node_modules/rolldown/bin/cli.mjs
